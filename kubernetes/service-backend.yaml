apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: dev-app
  labels:
    app: backend
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
